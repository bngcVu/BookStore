package com.bookstore.api.mapper;

import com.bookstore.api.entity.User;
import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface UserMapper {
    // Add methods as needed: toDto, fromDto
}
